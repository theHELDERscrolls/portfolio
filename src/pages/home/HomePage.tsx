import { QuoteCard } from "@/components";

export const HomePage = () => {
  return (
    <section className="flex items-center justify-center w-full bg-transparent h-dvh ">
      <QuoteCard />
    </section>
  );
};
